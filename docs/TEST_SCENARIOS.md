# 🧪 Шпаргалка по тестовым сценариям

## Быстрый доступ к командам

### Установка режимов переключателя

```yaml
# LOW (off/off) - скорость 30%
service: script.set_switch_low

# MID (on/off) - скорость 52%
service: script.set_switch_mid

# BOOST (off/on) - скорость 100%
service: script.set_switch_boost

# INVALID (on/on) - должна быть ошибка
service: script.set_switch_invalid
```

### Управление влажностью

```yaml
# Установить конкретное значение
service: input_number.set_value
target:
  entity_id: input_number.test_humidity
data:
  value: 85

# Быстро установить высокую влажность (85%)
service: input_boolean.turn_on
target:
  entity_id: input_boolean.quick_high_humidity

# Быстро установить нормальную влажность (60%)
service: input_boolean.turn_on
target:
  entity_id: input_boolean.quick_normal_humidity

# Постепенный рост влажности
service: input_boolean.turn_on
target:
  entity_id: input_boolean.simulate_high_humidity

# Остановить рост
service: input_boolean.turn_off
target:
  entity_id: input_boolean.simulate_high_humidity
```

### Прямое управление вентилятором (когда компонент готов)

```yaml
# Установить режим
service: smart_vent.set_mode
data:
  mode: low  # или mid, или boost

# Установить скорость напрямую
service: smart_vent.set_speed
data:
  percentage: 75

# Принудительный boost
service: smart_vent.force_boost
data:
  duration: 20  # минут (optional)
```

## 📋 Тестовые сценарии пошагово

### ✅ Сценарий 1: Базовое ручное управление

**Цель:** Проверить переключение режимов

1. Открой Developer Tools > Services
2. Вызови `script.set_switch_low`
3. Проверь: `fan.real_fan` должен быть на 30%
4. Вызови `script.set_switch_mid`
5. Проверь: `fan.real_fan` должен быть на 52%
6. Вызови `script.set_switch_boost`
7. Проверь: `fan.real_fan` должен быть на 100%

**Ожидаемое:** Все переключения работают мгновенно

---

### ✅ Сценарий 2: Автоматический boost по влажности

**Цель:** Проверить срабатывание auto-boost

1. Установи режим MID: `script.set_switch_mid`
2. Проверь текущую скорость: должна быть 52%
3. Установи высокую влажность:
   ```yaml
   service: input_boolean.turn_on
   target:
     entity_id: input_boolean.quick_high_humidity
   ```
4. Жди 20-30 секунд (интервал проверки)
5. Проверь:
   - `fan.real_fan` → 100% (boost)
   - `binary_sensor.smart_vent_auto_boost` → ON
6. Жди 20 минут (или измени таймер для теста)
7. Проверь:
   - Скорость вернулась к 52%
   - `binary_sensor.smart_vent_auto_boost` → OFF

**Ожидаемое:** Auto-boost активируется и деактивируется по таймеру

---

### ✅ Сценарий 3: Постепенный рост влажности

**Цель:** Симуляция реальной ситуации в ванной

1. Установи нормальную влажность: `input_boolean.quick_normal_humidity`
2. Установи режим MID
3. Включи симуляцию роста:
   ```yaml
   service: input_boolean.turn_on
   target:
     entity_id: input_boolean.simulate_high_humidity
   ```
4. Наблюдай за `input_number.test_humidity`
5. Когда достигнет >80%, должен сработать boost
6. Останови симуляцию:
   ```yaml
   service: input_boolean.turn_off
   target:
     entity_id: input_boolean.simulate_high_humidity
   ```

**Ожидаемое:** Boost срабатывает именно при 80%

---

### ✅ Сценарий 4: Отмена auto-boost переключателем

**Цель:** Проверить приоритет ручного управления

1. Активируй auto-boost (сценарий 2)
2. Проверь, что boost активен
3. Переключи в LOW: `script.set_switch_low`
4. Проверь немедленно:
   - Скорость = 30%
   - `binary_sensor.smart_vent_auto_boost` = OFF

**Ожидаемое:** Auto-boost отменяется мгновенно

---

### ✅ Сценарий 5: Ручной boost при активном auto-boost

**Цель:** Проверить приоритет ручного boost

1. Активируй auto-boost
2. Переключи в BOOST вручную: `script.set_switch_boost`
3. Проверь:
   - Скорость = 100%
   - `binary_sensor.smart_vent_auto_boost` = OFF (должен отмениться)
4. Жди >20 минут
5. Проверь: скорость остаётся 100% (ручной boost бесконечный)

**Ожидаемое:** Ручной boost отменяет автоматический

---

### ✅ Сценарий 6: Недопустимое состояние

**Цель:** Проверить обработку ошибок

1. Вызови: `script.set_switch_invalid`
2. Проверь логи: должна быть ERROR запись
3. Проверь: скорость автоматически установлена в 30% (LOW)

**Ожидаемое:** Система восстанавливается автоматически

---

### ✅ Сценарий 7: Лимит auto-boost в день

**Цель:** Проверить ограничение на количество boost

1. Установи в конфиге `max_boosts_per_day: 2` (для быстрого теста)
2. Активируй auto-boost (сценарий 2)
3. Дождись окончания boost
4. Активируй снова
5. Дождись окончания
6. Попробуй активировать 3-й раз
7. Проверь логи: должен быть WARNING о превышении лимита
8. Проверь: boost НЕ активировался

**Ожидаемое:** После 2-х boost третий блокируется

---

### ✅ Сценарий 8: Сброс счётчика в новый день

**Цель:** Проверить reset счётчика

1. Активируй максимум boost за день
2. Измени системное время на следующий день (в контейнере):
   ```bash
   docker exec ha-smart-vent-dev date -s "tomorrow"
   ```
3. Перезапусти HA
4. Попробуй активировать boost
5. Проверь: должен сработать (счётчик сброшен)

**Ожидаемое:** Счётчик сбрасывается в полночь

---

### ✅ Сценарий 9: Восстановление после перезагрузки (без boost)

**Цель:** Проверить restore_state

1. Установи режим MID
2. Перезапусти HA: `docker-compose restart`
3. После запуска проверь:
   - Режим остался MID
   - Скорость = 52%

**Ожидаемое:** Состояние восстановилось корректно

---

### ✅ Сценарий 10: Восстановление во время активного boost

**Цель:** Проверить restore_state с таймером

1. Активируй auto-boost
2. Через 5 минут перезапусти HA
3. После запуска проверь:
   - Boost всё ещё активен
   - Скорость = 100%
   - Таймер продолжает отсчёт (осталось ~15 мин)
4. Дождись окончания boost
5. Проверь: скорость вернулась к режиму переключателя

**Ожидаемое:** Таймер корректно восстанавливается

---

### ✅ Сценарий 11: Восстановление после истёкшего boost

**Цель:** Проверить обработку истёкшего boost при выключенном HA

1. Активируй auto-boost
2. Останови HA: `docker-compose stop`
3. Жди 25 минут
4. Запусти HA: `docker-compose start`
5. Проверь:
   - Boost НЕ активен
   - Скорость соответствует режиму переключателя
   - В логах запись о деактивации boost

**Ожидаемое:** Система понимает, что boost истёк

---

### ✅ Сценарий 12: Force boost через сервис

**Цель:** Проверить принудительный boost

1. Установи режим LOW
2. Вызови:
   ```yaml
   service: smart_vent.force_boost
   data:
     duration: 5  # 5 минут для быстрого теста
   ```
3. Проверь:
   - Скорость = 100%
   - `binary_sensor.smart_vent_auto_boost` = ON
4. Через 5 минут проверь:
   - Boost деактивировался
   - Скорость = 30% (LOW)

**Ожидаемое:** Force boost работает независимо от режима

---

## 🔍 Проверка логов

### Что искать в логах

```bash
# Все логи компонента
docker-compose logs | grep smart_vent

# Только ошибки
docker-compose logs | grep -i error | grep smart_vent

# Только важные события
docker-compose logs | grep -E "(boost|mode|humidity)" | grep smart_vent
```

### Ожидаемые записи в логах

**При смене режима:**
```
INFO: Mode changed from LOW to MID
INFO: Fan speed set to 52%
```

**При активации auto-boost:**
```
INFO: Auto-boost activated (humidity: 85%)
INFO: Fan speed set to 100%
INFO: Auto-boost count: 1/5
```

**При деактивации boost:**
```
INFO: Auto-boost ended (timeout)
INFO: Returning to mode: MID
INFO: Fan speed set to 52%
```

**При ошибке:**
```
ERROR: Invalid switch state detected (1/1)
WARNING: Setting safe mode: LOW
```

---

## 📊 Мониторинг через UI

### Entities для отслеживания

Создай в Lovelace dashboard:

```yaml
type: entities
title: Smart Vent Debug
entities:
  # Основные
  - entity: fan.smart_vent
  - entity: binary_sensor.smart_vent_auto_boost
  
  # Тестовые контроллеры
  - entity: input_number.test_humidity
  - entity: input_boolean.shelly_input_0
  - entity: input_boolean.shelly_input_1
  
  # Внутренние состояния
  - entity: fan.real_fan
  - entity: input_number.fan_speed
```

### Быстрые кнопки

```yaml
type: horizontal-stack
cards:
  - type: button
    name: LOW
    tap_action:
      action: call-service
      service: script.set_switch_low
  
  - type: button
    name: MID
    tap_action:
      action: call-service
      service: script.set_switch_mid
  
  - type: button
    name: BOOST
    tap_action:
      action: call-service
      service: script.set_switch_boost
  
  - type: button
    name: High Humidity
    tap_action:
      action: call-service
      service: input_boolean.turn_on
      target:
        entity_id: input_boolean.quick_high_humidity
```

---

## ✅ Чеклист готовности компонента

После прохождения всех сценариев:

- [ ] Все режимы переключаются корректно
- [ ] Auto-boost срабатывает при >80% влажности
- [ ] Auto-boost деактивируется через 20 минут
- [ ] Ручной режим имеет приоритет над автоматическим
- [ ] Недопустимые состояния обрабатываются
- [ ] Лимит boost в день работает
- [ ] Счётчик сбрасывается в полночь
- [ ] Restore state работает корректно
- [ ] Restore state с активным boost работает
- [ ] Force boost работает из любого режима
- [ ] Логи содержат все важные события
- [ ] Нет ERROR в логах (кроме тестовых)
- [ ] Все сервисы отвечают
- [ ] UI отображает корректное состояние

Если все чекбоксы отмечены - компонент готов к продакшену! 🎉
